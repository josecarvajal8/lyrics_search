import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        paddingVertical: 15
    },
    btnSongs: {
        padding: 5,
        borderWidth: 1
    }
});
export default styles;