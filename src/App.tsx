import React, { ReactNode } from 'react';
import { Text, View } from 'react-native';
import Router from './router';
const App: () => ReactNode = () => {
    return (
        <Router />
    )
}

export default App;